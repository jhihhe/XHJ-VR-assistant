# 象视助手页面适配草案

状态：部分页面已核对，完整抓取与浏览器回归被自动审批拦截，不能作为已验证发布版。

采用保留现有业务模块、修正页面识别并统一排版的方案。相比完全重写，更容易保留表格 Hook、15 套主题、上传计数与重试、同步、缩放、本地验证码识别及已保存的设置。仅追加零散样式则无法修正当前活动 iframe 判断错误。

设计规范：沿用 Apple Light 默认主题、系统中文字体、蓝色主操作和明确的危险操作色。间距按 4/8/12/16/24px 组织；桌面常用控件 36px，窄屏至少 44px。表单标签按行对齐，个人资料保持纵向字段组；弹窗标题给关闭按钮预留空间，上传操作组允许换行。保留原生隐藏状态与事件绑定。

已观察：主壳使用 #admin-body，活动标签页使用 .layui-tab-item.layui-show；同时存在个人资料与房勘列表 iframe。旧代码选择第一个 iframe，会把房勘列表误识别为个人资料。房勘操作包含上传、取消、转单、同步、详情；有效记录还包含无效操作。不会为验证效果触发这些业务写入。

已保存：work/xhj-refresh/assets 下 4 个 CSS 文件、1 个字体以及来源清单。主壳 DOM 与房勘列表结构已读取，但未保存完整 HTML；全景、数据中心和上传页面尚未重新核对。网页抓取仅涵盖浏览器可获取的前端代码，不包括服务器端源码。

后续验证：恢复访问后保存各页面 HTML、内联与外部脚本，检查上传窗口和所有主题，覆盖 1440/1024/390px 下的按钮文字、分页、弹窗关闭按钮和横向滚动；再决定发布。
